@using Microsoft.AspNetCore.Http
@{
    string controller = ViewContext.RouteData.Values["Controller"].ToString();
    if (controller == "SpendingTracker")
    {
        Layout = "SpenderLayout";
    }
    else
    {
        Layout = "_Layout";
    }
    
}
